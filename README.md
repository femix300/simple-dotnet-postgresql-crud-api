# Simple .NET PostgreSQL CRUD API

A basic .NET 9 Web API demonstrating CRUD operations with PostgreSQL using Entity Framework Core. Copy `appsettings.json.template` to `appsettings.json` and add your PostgreSQL connection string to get started.
